version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${HOST_PORT:-3002}:3000"
    volumes:
      # Mount the sqlite database file outside the container to persist it
      - ./prisma/dev.db:/app/prisma/dev.db
    environment:
      - NODE_ENV=production
